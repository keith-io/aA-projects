import { merge } from 'lodash';

export const uiReducer = (state = {}, action) => {
  switch (action.type) {
   // case somecase:
      // do something
      // return {};
    default:
      return state;
  }
};